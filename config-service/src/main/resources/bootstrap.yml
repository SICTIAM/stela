spring:
  application:
    name: config-service
  cloud:
    config:
      server: 
       git:
          uri: ssh://git@forge.sictiam.fr:6666/internet/stela-config.git
          cloneOnStart: true 
          default-label: feature/4139-Mettre-en-place-un-meilleur-moyen-d-explorer-les-logs        
server:
  port: 8888

---

spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          search-locations: file:../stela-config
          
---

spring:
  profiles: prod
  cloud:
    config:
      server: 
       git:
          default-label: master    
