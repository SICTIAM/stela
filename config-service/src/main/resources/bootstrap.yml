spring:
  application:
    name: config-service
  profiles:
    active: default
  cloud:
    config:
      server:
        git:
          uri: file://stela-config
          # Need to have SSH host key locally in RSA format
          #uri: ssh://git@forge.sictiam.fr:6666/internet/stela-config.git
          default-label: develop

server:
  port: 8888
