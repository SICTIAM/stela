server:
  port: 9004

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://config:8888
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30

---

spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888

